# ZQ RAG Python：容器部署与 BM25 配置

> 整理自前面的服务器部署记录。本文包含 PostgreSQL、Redis、MinIO、BM25、数据目录、密码、Windows 连接和安全组配置。
>
> 重要：本文包含开发环境密码示例，不要提交到 Git 或发到公开位置。生产环境建议重新生成并轮换所有密码。

## 1. 目标部署结构

| 服务 | Docker 容器 | 镜像 | 宿主机端口 | 用途 |
|---|---|---|---:|---|
| PostgreSQL + pgvector + BM25 | rag-postgres | paradedb/paradedb:0.25.6-pg17 | 5432 | 业务数据库、向量检索、BM25 全文检索 |
| Redis | rag-redis | redis:7-alpine | 6379 | 缓存、异步任务队列 |
| MinIO | rag-minio | minio/minio | 9000、9001 | 文件对象存储、MinIO 控制台 |

服务器公网 IP：103.236.92.173

Python 项目：D:\code\zq-rag-py

数据库名和用户：ragkb

## 2. 当前约定的账号密码

### PostgreSQL

    数据库：ragkb
    用户：ragkb
    密码：RagKb_2026_Strong
    端口：5432

### Redis

    密码：redis6379
    端口：6379

### MinIO

    Root User：minioadmin
    Root Password：minio9000
    API 端口：9000
    控制台端口：9001

### DashScope

DashScope API Key 不在本文中保存完整值。使用阿里云控制台中实际生成的完整 Key：

    DASHSCOPE_API_KEY=你的完整DashScopeKey

## 3. 数据目录

### PostgreSQL 数据目录

当前 PostgreSQL 使用宿主机目录挂载：

    宿主机：/data/rag-postgres
    容器内：/var/lib/postgresql/data

这个目录是真实数据库数据目录，不要删除、格式化或执行 rm -rf。

### 备份目录

    /data/rag-backup

### 推荐的 Redis 和 MinIO 数据目录

如果重新创建容器，建议使用：

    Redis：/data/rag-redis -> /data
    MinIO：/data/rag-minio -> /data

查看当前容器的实际挂载目录：

~~~bash
docker inspect rag-postgres --format '{{range .Mounts}}{{println .Source "->" .Destination}}{{end}}'
docker inspect rag-redis --format '{{range .Mounts}}{{println .Source "->" .Destination}}{{end}}'
docker inspect rag-minio --format '{{range .Mounts}}{{println .Source "->" .Destination}}{{end}}'
~~~

## 4. 新服务器初始化 Docker

以下命令在 Ubuntu 服务器的 root 终端执行。

### 4.1 创建数据目录

~~~bash
mkdir -p /data/rag-postgres
mkdir -p /data/rag-redis
mkdir -p /data/rag-minio
mkdir -p /data/rag-backup
~~~

### 4.2 设置 Docker 开机自启动

~~~bash
systemctl enable --now docker
systemctl status docker --no-pager
~~~

## 5. 部署 PostgreSQL、pgvector 和 BM25

普通的 pgvector/pgvector:pg17 镜像只提供 pgvector，不提供 pg_search，因此 BM25 使用 ParadeDB 镜像。

### 5.1 下载 ParadeDB 镜像

~~~bash
docker pull paradedb/paradedb:0.25.6-pg17
~~~

### 5.2 创建 PostgreSQL 容器

~~~bash
docker run -d \
  --name rag-postgres \
  --restart unless-stopped \
  --shm-size=1g \
  -e POSTGRES_DB=ragkb \
  -e POSTGRES_USER=ragkb \
  -e POSTGRES_PASSWORD='RagKb_2026_Strong' \
  -p 0.0.0.0:5432:5432 \
  -v /data/rag-postgres:/var/lib/postgresql/data \
  paradedb/paradedb:0.25.6-pg17
~~~

--restart unless-stopped 表示 Docker 服务启动后容器自动启动。

### 5.3 配置 pg_search 预加载

pg_search 不仅要执行 CREATE EXTENSION，还必须加入 PostgreSQL 的 shared_preload_libraries。

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -v ON_ERROR_STOP=1 \
  -c "ALTER SYSTEM SET shared_preload_libraries = 'pg_search';"

docker restart rag-postgres
sleep 8
~~~

验证：

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -c "SHOW shared_preload_libraries;"
~~~

应该看到 pg_search。

### 5.4 创建 PostgreSQL 扩展

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -v ON_ERROR_STOP=1 \
  -c "CREATE EXTENSION IF NOT EXISTS vector; CREATE EXTENSION IF NOT EXISTS pg_search;"
~~~

验证扩展：

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -c "SELECT extname, extversion FROM pg_extension WHERE extname IN ('vector','pg_search');"
~~~

预期能看到 vector 和 pg_search。

## 6. 当前服务器的数据库迁移

当前实际情况：

- ragkb 数据库为空；
- 表结构位于 postgres 数据库；
- postgres 中有 10 张业务表；
- 应用配置使用 DB_NAME=ragkb。

因此需要把 postgres 数据库中的表和数据复制到 ragkb。

### 6.1 导出 postgres 数据库

~~~bash
RAG_MIGRATION_DUMP="/data/rag-backup/postgres-to-ragkb-$(date +%F-%H%M%S).sql"

docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  pg_dump -U ragkb -d postgres \
  --no-owner --no-privileges > "$RAG_MIGRATION_DUMP"

test -s "$RAG_MIGRATION_DUMP" && ls -lh "$RAG_MIGRATION_DUMP"
~~~

### 6.2 导入到 ragkb

此前确认 ragkb 没有业务表，可以导入：

~~~bash
docker exec -i -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -v ON_ERROR_STOP=1 < "$RAG_MIGRATION_DUMP"
~~~

确认表已经进入正确数据库：

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb -c "\dt"
~~~

应该看到：

    kb_answer_feedback
    kb_chat_message
    kb_chat_session
    kb_doc_chunk
    kb_document
    kb_eval_dataset
    kb_eval_result
    kb_index_task
    kb_knowledge_base
    kb_permission

原来的 postgres 数据库先不要删除，确认 Python 项目正常后再处理。

## 7. 创建 BM25 索引

确认 ragkb 中存在 kb_doc_chunk 后执行：

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -v ON_ERROR_STOP=1 \
  -c "CREATE INDEX IF NOT EXISTS idx_chunk_bm25 ON kb_doc_chunk USING bm25 (id, content, section_title, kb_id, doc_id, doc_version) WITH (key_field = 'id');"
~~~

验证：

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -c "SELECT indexname, indexdef FROM pg_indexes WHERE indexname = 'idx_chunk_bm25';"
~~~

BM25 查询示例：

~~~sql
SELECT
    id,
    content,
    pdb.score(id) AS bm25_score
FROM kb_doc_chunk
WHERE content ||| 'PostgreSQL'
ORDER BY bm25_score DESC
LIMIT 5;
~~~

原来的 content_tsv + GIN 索引可以保留，BM25 和向量 HNSW 索引可以一起使用。

## 8. Redis 容器

如果是新服务器或需要重建 Redis：

~~~bash
docker pull redis:7-alpine

docker run -d \
  --name rag-redis \
  --restart unless-stopped \
  -p 0.0.0.0:6379:6379 \
  -v /data/rag-redis:/data \
  redis:7-alpine \
  redis-server --appendonly yes --requirepass 'redis6379'
~~~

验证：

~~~bash
docker exec rag-redis redis-cli -a 'redis6379' ping
~~~

预期：PONG。

## 9. MinIO 容器

如果是新服务器或需要重建 MinIO：

~~~bash
docker pull minio/minio

docker run -d \
  --name rag-minio \
  --restart unless-stopped \
  -p 0.0.0.0:9000:9000 \
  -p 0.0.0.0:9001:9001 \
  -e MINIO_ROOT_USER='minioadmin' \
  -e MINIO_ROOT_PASSWORD='minio9000' \
  -v /data/rag-minio:/data \
  minio/minio server /data --console-address ':9001'
~~~

控制台：

    http://103.236.92.173:9001

API：

    http://103.236.92.173:9000

## 10. 所有容器检查

~~~bash
docker ps
docker ps -a
docker exec rag-postgres pg_isready -U ragkb -d ragkb
docker logs --tail 100 rag-postgres
docker logs --tail 100 rag-redis
docker logs --tail 100 rag-minio
~~~

## 11. 备份和恢复

### 11.1 备份整个 PostgreSQL 实例

~~~bash
mkdir -p /data/rag-backup

docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  pg_dumpall -U ragkb > "/data/rag-backup/rag-postgres-$(date +%F-%H%M%S).sql"

ls -lh /data/rag-backup
~~~

备份文件必须大于 0 字节，空文件不能视为有效备份。

### 11.2 恢复单个数据库

~~~bash
docker exec -i -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb -v ON_ERROR_STOP=1 < /data/rag-backup/备份文件.sql
~~~

恢复前应确认目标数据库和表状态，避免重复导入。

## 12. Collation 版本警告

曾出现：

    database "ragkb" has a collation version mismatch
    The database was created using collation version 2.36,
    but the operating system provides version 2.41.

这不是连接失败，也不是 BM25 失败原因，数据库仍然可以正常接受连接。

在维护窗口执行重建：

~~~bash
docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -v ON_ERROR_STOP=1 \
  -c "REINDEX DATABASE ragkb;"

docker exec -e PGPASSWORD='RagKb_2026_Strong' rag-postgres \
  psql -U ragkb -d ragkb \
  -v ON_ERROR_STOP=1 \
  -c "ALTER DATABASE ragkb REFRESH COLLATION VERSION;"
~~~

REINDEX DATABASE 可能锁定写入并耗时，数据量大时不要在业务高峰执行。不能只执行 REFRESH COLLATION VERSION 而不重建受影响的索引。

## 13. Python 项目 .env

文件位置：

    D:\code\zq-rag-py\.env

数据库和基础设施配置：

~~~env
DB_HOST=103.236.92.173
DB_PORT=5432
DB_NAME=ragkb
DB_USERNAME=ragkb
DB_PASSWORD=RagKb_2026_Strong

REDIS_HOST=103.236.92.173
REDIS_PORT=6379
REDIS_PASSWORD=redis6379
REDIS_DB=0

MINIO_ENDPOINT=http://103.236.92.173:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minio9000
MINIO_BUCKET=rag-documents
~~~

DashScope 配置：

~~~env
DASHSCOPE_API_KEY=你的完整DashScopeKey

# 如果 API Key 属于业务空间，优先使用控制台显示的专属 OpenAI 兼容地址
OPENAI_BASE_URL=https://llm-ep48etgbpj8lhiqo.cn-beijing.maas.aliyuncs.com/compatible-mode/v1

CHAT_MODEL=qwen-plus
CHAT_TEMPERATURE=0.1
CHAT_MAX_TOKENS=2048
EMBEDDING_MODEL=text-embedding-v3
~~~

项目还要求配置：

~~~env
RERANKER_ENDPOINT=你的重排模型地址
JWT_SECRET_KEY=请生成一串随机长密钥
~~~

Python 的 Pydantic Settings 会自动读取 .env 中的 DB_HOST、DB_USERNAME 等大写变量。当前代码读取的是 DB_USERNAME，不要写成 DB_USER。

启动时要在项目根目录执行：

~~~powershell
cd D:\code\zq-rag-py
uv run uvicorn zq_rag_app.main:app --host 0.0.0.0 --port 8000 --reload
~~~

## 14. Windows 连接服务器

### 14.1 PowerShell 测试 PostgreSQL 端口

Test-NetConnection 是 PowerShell 命令，不是 CMD 命令：

~~~powershell
Test-NetConnection 103.236.92.173 -Port 5432
~~~

看到 TcpTestSucceeded : True，说明端口可以访问。

### 14.2 PostgreSQL GUI 配置

    主机：103.236.92.173
    端口：5432
    初始数据库：ragkb
    用户名：ragkb
    密码：RagKb_2026_Strong

不要继续使用 postgres 作为初始数据库；当前 Python 项目使用 ragkb。

### 14.3 Redis GUI 配置

    主机：103.236.92.173
    端口：6379
    密码：redis6379

## 15. 云服务器安全组

建议入方向规则：

| 端口 | 协议 | 授权来源 |
|---:|---|---|
| 22 | TCP | 管理员固定公网 IP /32 |
| 5432 | TCP | Windows 调试机或应用服务器公网 IP /32 |
| 6379 | TCP | 仅应用服务器 IP，平时不要对公网开放 |
| 9000 | TCP | 仅应用服务器 IP，平时不要对公网开放 |
| 9001 | TCP | 管理员固定公网 IP /32 |
| 8000 | TCP | 需要直接访问 API 时才开放 |

例如 Windows 当前公网 IP 如果是 58.252.94.141，单个来源写：

    58.252.94.141/32

不要把 PostgreSQL、Redis、MinIO 配置成 0.0.0.0/0 长期暴露。密码认证不能替代安全组和防火墙。

出方向至少允许 TCP 80/443，以便下载 Docker 镜像和访问 DashScope API。

## 16. 当前问题的正确处理顺序

1. 确认 rag-postgres 使用 paradedb/paradedb:0.25.6-pg17。
2. 确认 /data/rag-postgres 挂载到 /var/lib/postgresql/data。
3. 配置 shared_preload_libraries = 'pg_search' 并重启容器。
4. 在 ragkb 数据库创建 pg_search 扩展。
5. 将错误导入到 postgres 数据库的表和数据迁移到 ragkb。
6. 在 ragkb.kb_doc_chunk 创建 idx_chunk_bm25。
7. 将 Python .env 的 DB_NAME 设置为 ragkb。
8. 用 pg_isready、扩展查询和 BM25 查询验证。
9. 最后再处理 collation 警告和旧容器清理。

## 17. 重要禁止事项

不要执行：

~~~bash
rm -rf /data/rag-postgres
docker compose down -v
~~~

也不要在新 rag-postgres 运行时启动旧的 rag-postgres-pgvector-old，因为两个容器不能同时占用同一个 5432 端口和同一个 PostgreSQL 数据目录。

旧容器应该先保留，确认新容器、Python 应用和数据都正常后再决定是否清理。

## 18. 官方参考

- [ParadeDB Docker 镜像](https://hub.docker.com/r/paradedb/paradedb/tags)
- [ParadeDB GitHub](https://github.com/paradedb/paradedb)
- [ParadeDB BM25 文档](https://www.paradedb.com/learn/search-in-postgresql/bm25)
- [PostgreSQL Collation 文档](https://www.postgresql.org/docs/17/sql-altercollation.html)
